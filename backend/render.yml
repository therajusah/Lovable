services:
  - type: web
    name: lovable-backend
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
      - key: E2B_API_KEY
        sync: false
      - key: GOOGLE_API_KEY
        sync: false
